# Local contributor default. Set VITE_DATA_MODE=supabase in Netlify.
VITE_DATA_MODE=local
# Use an isolated, synthetic tenant for any public portfolio deployment.
VITE_RESTAURANT_SLUG=sajitap-demo
# Browser-public values only. Never use a service-role key in VITE_*.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
